{{ if eq .chezmoi.os "darwin" -}}
#!/bin/bash

brew bundle --no-lock --file=/dev/stdin <<EOF

brew "tmux"
brew "htop"
brew "jq"

# Required for nvim
brew "neovim"
brew "ripgrep"
brew "lazygit"

cask "miniforge"

EOF
{{ end -}}
